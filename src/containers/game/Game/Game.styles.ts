import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  container: {
    padding: "10px 0",
    margin: "5px 0",
    textAlign: "center"
  },
  time: { color: "#424242" },
  action: {
    color: "#fff",
    backgroundColor: "#009788"
  }
});

import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  timeContainer: {
    padding: "10px 0",
    margin: "5px 0",
    color: "#424242",
    textAlign: "center"
  },
  time: { color: "#424242" }
});

import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  root: {
    backgroundColor: "#009688",
    color: "#fff",
    position: "absolute",
    right: 24,
    bottom: 24
  }
});
